# [#](#产品定价) 产品定价

## [#](#计费模式) 计费模式

微信云托管下云环境均为按量付费模式，先用后付，每个环境都会独立计费。系统会每日进行资源消耗和结算并在次日（特殊情况下可能延迟几天，但不会超过当月最后一天）进行扣费。
开发者可前往「微信云托管控制台-费用中心-充值与账单」进行充值或查看账单信息。

**首个环境系统会赠送一定免费额度**供开发者使用，该免费额度有效期为3个月。3个月内超过免费额度的资源消耗及3个月免费额度到期后即会按照实际使用量进行对应的刊例价扣除费用。

刊例价及首次免费额度信息如下：

| 所属功能 | 参数 | 价格 | 免费额度（有效期3个月） |
| --- | --- | --- | --- |
| 容器（服务） | CPU使用量 | 0.055元/（核 \*小时 ） | 720（核 \*小时） |
| 容器（服务） | 内存使用量 | 0.032元/（GB \*小时） | 1440（GB \*小时） |
| 容器（服务） | 构建时长 | 0.05元/分钟 | 600 分钟 |
| 容器（服务） | 公网流量 | 0.8元/GB | 5GB |
| MySQL | 数据库算力 | 0.342元/（个 \*小时） | 720（个 \*小时） |
| MySQL | 数据库存储 | 0.00485元/（GB \*小时） | 720（GB \*小时） |
| 对象存储 | 容量 | 0.0043元/（GB \*天） | 5GB |
| 对象存储 | 下载操作次数 | 0.01元/万次 | 6000万次 |
| 对象存储 | 上传操作次数 | 0.01元/万次 | 3000万次 |
| 对象存储 | CDN回源流量 | 0.15元/GB | 3GB |
| 对象存储 | CDN流量 | 0.18元/GB | 3GB |
| 静态资源存储 | 容量 | 0.0043元/（GB\*天） | 无 |
| 静态资源存储 | 流量 | 0.21元/GB | 无 |

## [#](#计费参数说明) 计费参数说明

| 所属功能 | 参数 | 产生用量条件 | 备注 |
| --- | --- | --- | --- |
| 容器（服务） | CPU（核×小时） | 环境中任意服务下任意版本，实例数量不为0 | 1（核×小时）表示1核的 CPU 连续运行1个小时所消耗的资源量，或2核的 CPU 连续运行30分钟所消耗的资源量。1（核×小时） = 60（核×分钟） = 3600（核×秒） |
| 容器（服务） | 内存（GiB×小时） | 环境中任意服务下任意版本，实例数量不为0 | 1（GiB×小时）表示 1GiB 的内存连续运行一小时所消耗的资源量，或 2GiB 的内存连续运行30分钟所消耗的资源量。1（GiB×小时）= 60（GiB×分钟）= 3600（GiB×秒） |
| 容器（服务） | 流量（GB） | 环境中任意服务通过公网域名访问（会产生公网回包），或任意服务访问过公网（会产生出流量） | 只通过callcontainer访问服务不产生用量 |
| 容器 （服务） | 构建（分钟） | 发布服务时，采用了「本地代码/代码库拉取」方式 | 使用「镜像拉取」方式无构建用量，但需要消耗本地或别处的机器资源自行打包容器镜像 |
| MySQL | 数据库算力(个\*小时) | 对数据库进行了任意读写/运算操作，或通过任意管理界面登录访问过数据库 | 1 CCU 等价于1核2GB的数据库的计算能力 |
| MySQL | 数据库存储（GB \* 小时） | 开通了MySQL未注销就会持续产生用量 | 无论是否存有业务数据，数据库系统表都至少消耗28M存储空间 |
| 对象存储 | 存储容量（GB\*天） | 对象存储中存在任意文件 | 按照存储容量和存储时长进行计算 |
| 对象存储 | 下载操作次数（万次） | 使用 API、SDK 或控制台等方式进行数据下载操作 | 无论请求成功或失败均计算次数，不足一万次按一万次计算 |
| 对象存储 | 上传操作次数（万次） | 使用 API、SDK 或控制台等方式进行数据上传操作 | 无论请求成功或失败均计算次数，不足一万次按一万次计算 |
| 对象存储 | CDN流量 | 调用过对象存储中的任意文件 | 对象存储默认开启了CDN加速，只要调用即会产生 |
| 对象存储 | CDN回源流量 | 调用过对象存储中的任意文件，触发了CDN回源 | 调用时无法预判是否会产生CDN回源，与CDN缓存有关 |
| 静态资源存储 | 容量（GB \* 天） | 静态资源存储中存在任意文件 | 按照存储容量和存储时长进行计算 |
| 静态资源存储 | 流量 （GB） | 调用过静态资源存储中的任意文件 | 静态资源存储默认开启了CDN加速，只要调用即会产生，但不区分是否CDN回源 |

## [#](#与传统模式价格对比) 与传统模式价格对比

按刊例价，计算常见容器实例规格费用：

> 运行不足一小时按实际运行时间计算，计费时间粒度精确到秒
> 实例规格每个服务可能都不一样，需要分别手动指定

| 实例规格 | 价格 |
| --- | --- |
| 0.25核0.5G | 0.02975元/小时 |
| 0.5核1G | 0.0595元/小时 |
| 1核1G | 0.087元/小时 |
| 1核2G | 0.119元/小时 |
| 2核4G | 0.238元/小时 |
| 4核8G | 0.476元/小时 |

按刊例价，计算常见数据库费用：

> 运行不足一小时按实际运行时间计算，计费时间粒度精确到秒
> 数据库规格无需手动指定，会自动弹性伸缩

| 数据库算力 | 对应传统数据库规格 | 价格 |
| --- | --- | --- |
| 0.25 | 0.25核0.5G | 0.0855元/小时 |
| 0.5 | 0.5核1G | 0.171元/小时 |
| 1 | 1核2G | 0.342元/小时 |
| 2 | 2核4G | 0.684元/小时 |
| 4 | 4核8G | 1.368元/小时 |
| 8 | 8核16G | 2.736元/小时 |
| 16 | 16核32G | 5.472元/小时 |

## [#](#增值服务) 增值服务

为支持用户更多业务特殊需求，满足该特殊需求的云托管能力将以增值服务的形式对外提供，因支持特殊需求涉及消耗底层资源，现增值服务以包年包月付费模式进行计费，先付后用，每个环境都会独立计费。可前往「微信云托管控制台-设置-环境设置-增值服务」中进行购买，系统会在购买增值服务时进行扣费。
开发者可前往「微信云托管控制台-费用中心-充值与账单」进行充值或查看账单信息。

### [#](#出口固定IP) 出口固定IP

| 服务 | 价格 | 说明 |
| --- | --- | --- |
| 出口固定IP | 49元/（月\*环境） | 出口固定IP可用作调用外部接口允许访问IP，如政企接口、微信支付提现到零钱接口需要使用方提供固定IP等场景，本固定IP不可用作域名备案 |

- 购买后不支持退款，特殊情况如删除环境，固定IP购买有效期未到期，平台将按照剩余有效时间比例进行退款；
- 出口固定IP不可用作域名备案；
- 增值服务为环境级别服务，只提供给当前环境使用；
- 购买成功、即将到期、到期，均会发送微信服务通知；
- 服务到期后后未进行续费，该固定IP将回收不可用；
- 到期未续费，后续再次购买，IP随机；到期续费，IP保持不变；

Incorrect translation.