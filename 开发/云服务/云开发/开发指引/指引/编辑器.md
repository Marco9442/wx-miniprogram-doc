# [#](#编辑器中操作云开发云函数) 编辑器中操作云开发云函数

在项目根目录找到 `project.config.json` 文件，新增 `cloudfunctionRoot` 字段，指定本地已存在的目录作为云开发的本地根目录。

> **如果新建项目使用云开发模板则无需手动填写**

示例：

```
{
   "cloudfunctionRoot": "cloudfunctions/"
}
```

Incorrect translation.