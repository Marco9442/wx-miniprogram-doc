# [#](#AnimationObject-worklet-delay-number-delayMS-AnimationObject-delayedAnimation) AnimationObject worklet.delay(number delayMS, AnimationObject delayedAnimation)

> **小程序插件**：不支持

> 相关文档: [worklet 动画](../../../../framework/runtime/skyline/worklet.html)

## [#](#功能描述) 功能描述

延迟执行动画。

## [#](#参数) 参数

### [#](#number-delayMS) number delayMS

动画开始前等待的时间，单位：毫秒。

### [#](#AnimationObject-delayedAnimation) AnimationObject delayedAnimation

动画对象。

## [#](#返回值) 返回值

### [#](#AnimationObject) AnimationObject

返回 `AnimationObject` 类型值，可直接赋值给 `SharedValue`。

## [#](#示例代码) 示例代码

```
const { shared, repeat, timing } = wx.worklet
const offset = shared(0)
offset.value = delay(1000, timing(70));
```

Incorrect translation.