自定义上报/隐私遮罩/
# [\#](https://developers.weixin.qq.com/miniprogram/analysis/experience/sdk/privacy.html\#%E9%9A%90%E7%A7%81) 隐私
## [\#](https://developers.weixin.qq.com/miniprogram/analysis/experience/sdk/privacy.html\#%E9%81%AE%E7%BD%A9) 遮罩
采集模块提供了对图片、视频、文本内容的遮罩功能。对需要遮罩的图片和视频，采集模块会将原图片或视频替换成骨架图上报，文本则替换成星号。
开发者可根据需要，在 setup 时配置 maskMode 来选择白名单遮罩模式或黑名单遮罩模式。
在白名单模式下，SDK 会默认遮罩所有的元素，开发者可以在元素上添加类 `wxobs-unmask` 来解除该元素及其子元素的遮罩。
如
```html
...
```
在黑名单模式下，所有元素默认不遮罩。开发者可以针对敏感元素，在元素上添加类 `wxobs-mask` 来遮罩该元素及其子元素。
如
```html
...
```
## [\#](https://developers.weixin.qq.com/miniprogram/analysis/experience/sdk/privacy.html\#%E5%B1%8F%E8%94%BD) 屏蔽
为保护用户隐私，采集模块屏蔽了以下数据：
- 所有 input 事件
- open-data
- 小程序插件内容
此外，我们也向开发者提供自定义屏蔽元素的方法：
在元素上添加类 `wxobs-exclude`，该元素将不会被采集。例如：
```html
...
```